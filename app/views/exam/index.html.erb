<%= stylesheet_link_tag "//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" %>
<%= javascript_include_tag "//ajax.googleapis.com/ajax/libs/angularjs/1.4.5/angular.min.js" %>
<%= javascript_include_tag 'exam' %>
<div ng-controller="BoardController as board" >
	<div class="box">
		<h3>Connect Four</h3>
		<table class="table">
			<tr>
				<th ng-click="board.answer(0, 1)"><span class="glyphicon glyphicon-circle-arrow-down"></span></th>
				<th ng-click="board.answer(1, 1)"><span class="glyphicon glyphicon-circle-arrow-down"></span></th>
				<th ng-click="board.answer(2, 1)"><span class="glyphicon glyphicon-circle-arrow-down"></span></th>
				<th ng-click="board.answer(3, 1)"><span class="glyphicon glyphicon-circle-arrow-down"></span></th>
				<th ng-click="board.answer(4, 1)"><span class="glyphicon glyphicon-circle-arrow-down"></span></th>
				<th ng-click="board.answer(5, 1)"><span class="glyphicon glyphicon-circle-arrow-down"></span></th>
				<th ng-click="board.answer(6, 1)"><span class="glyphicon glyphicon-circle-arrow-down"></span></th>
			</tr>
			<tr ng-repeat="(y, row) in board.grid">
				<td ng-repeat="(x, col) in board.grid[y] track by x"><span class="disc disc{{col}}">{{col}}</span></td>
			</tr>
		</table>
		<p> This is a two player game. Every players goal is to make 4 in a row either horizontally, vertically or diagonally.</p>
	</div>
</div>
<style>
	.table th {
		text-align: center
	}
	.box {
		margin: 100px auto;
		width: 400px;
		text-align: center
	}
	.disc {
		padding: 4px 8px;
		border-radius: 20px;
	}
	.disc0 {
		background: #f0f0f0;
	}
	.disc1 {
		background: #f33;
	}
	.disc2 {
		background: #aaa;
		border-radius: 20px;
	}
</style>